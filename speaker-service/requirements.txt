# Torch is intentionally not pinned here. Install the build that matches your CUDA version
# first (see pytorch.org), then this file. torchaudio must come from the same index/build as
# torch (funasr.utils.load_utils imports it unconditionally).
funasr==1.4.1
websockets>=15.0
numpy>=1.24
# Overlap detection (overlap.py). Optional at runtime: without HF_TOKEN, load_model() returns
# None and the service runs without it.
pyannote.audio>=4.0.7
